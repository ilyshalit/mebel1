# 🛋️ Виртуальная Примерка Мебели

AI-powered система для виртуального размещения мебели в интерьере.

## 🎯 Возможности

- ✅ Загрузка фото комнаты и мебели
- ✅ Автоматическое размещение мебели (AI выбирает место)
- ✅ Ручное размещение (пользователь выбирает область)
- ✅ Каталог мебели для выбора
- ✅ Умные рекомендации доп товаров (upsell)
- ✅ Сохранение оригинальных деталей комнаты

## 🏗️ Архитектура (Вариант A - MVP)

```
Frontend (HTML/JS)
    ↓
FastAPI Backend
    ↓
├─ GPT-4 Vision (анализ сцены + создание детального промпта)
├─ rembg (удаление фона мебели)
└─ Qwen Image Edit на Kie.ai (семантическое редактирование)
```

## 📋 Требования

- Python 3.9+
- Node.js (для frontend разработки, опционально)
- API ключи:
  - OpenAI (GPT-4 Vision)
  - Kie.ai (для Qwen Image Edit)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните API ключи:

```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваши ключи:
```
OPENAI_API_KEY=sk-...
REMOVEBG_API_KEY=   # Оставьте пустым (используется rembg)
KIE_AI_API_KEY=...  # Получите на kie.ai
```

**Подробная инструкция:** См. [KIE_AI_SETUP.md](KIE_AI_SETUP.md)

### 3. Запуск сервера

```bash
cd backend
uvicorn app:app --reload --host 0.0.0.0 --port 8000
```

### 4. Открытие интерфейса

Откройте `frontend/index.html` в браузере или запустите локальный сервер:

```bash
cd frontend
python -m http.server 8080
```

Перейдите на http://localhost:8080

## 📁 Структура проекта

```
furniture-placement/
├── backend/
│   ├── app.py                  # FastAPI приложение
│   ├── services/
│   │   ├── gpt4_analyzer.py    # Анализ изображений с GPT-4V
│   │   ├── background_remover.py # Удаление фона
│   │   ├── inpainting.py       # Stable Diffusion Inpainting
│   │   └── upsell.py           # Система рекомендаций
│   ├── models/
│   │   └── schemas.py          # Pydantic схемы
│   ├── utils/
│   │   ├── image_utils.py      # Утилиты для работы с изображениями
│   │   └── load_env.py         # Загрузка env переменных
│   └── requirements.txt
├── frontend/
│   ├── index.html              # Главная страница
│   ├── css/
│   │   └── style.css           # Стили
│   └── js/
│       └── app.js              # Логика интерфейса
├── data/
│   ├── uploads/                # Загруженные изображения
│   ├── results/                # Результаты генерации
│   └── catalog/                # Каталог мебели
├── .env.example
└── README.md
```

## 🎨 Использование

### Режим 1: Автоматическое размещение

1. Загрузите фото комнаты
2. Загрузите фото мебели или выберите из каталога
3. Нажмите "Разместить автоматически"
4. AI сам определит лучшее место и разместит мебель

### Режим 2: Ручное размещение

1. Загрузите фото комнаты
2. Загрузите фото мебели или выберите из каталога
3. Нажмите "Выбрать место вручную"
4. Кликните на области комнаты где хотите разместить мебель
5. AI разместит мебель в указанном месте

## 💰 Стоимость использования

- GPT-4V: ~$0.01 за анализ
- rembg: Бесплатно (работает локально)
- Qwen Image Edit: ~$0.0125 за генерацию
- **Итого: ~$0.02-0.03 за полную генерацию**

На $20 можно сделать ~700-1000 генераций!

## 🔄 Апгрейд до Варианта B (продвинутая версия)

Когда проект окупится, можно улучшить качество:

```bash
# В backend/services/ заменить:
# background_remover.py → segment_anything.py
# inpainting.py → controlnet_inpainting.py

# Установить дополнительные зависимости:
pip install segment-anything-py
```

Детали в `UPGRADE_TO_B.md`

## 🛠️ API Endpoints

### POST `/api/analyze`
Анализ изображений комнаты и мебели

### POST `/api/generate`
Генерация результата с размещенной мебелью

### POST `/api/upsell`
Получение рекомендаций доп товаров

### GET `/api/catalog`
Список мебели из каталога

## 📝 TODO для продакшена

- [ ] Добавить аутентификацию пользователей
- [ ] Кеширование результатов
- [ ] Очередь для обработки (Celery/Redis)
- [ ] Мобильная версия
- [ ] Интеграция с CMS заказчика
- [ ] Telegram бот
- [ ] Метрики и аналитика

## 🤝 Поддержка

Для вопросов и предложений создайте issue в репозитории.

## 📄 Лицензия

Проприетарная. Все права защищены.
