# 🚀 Быстрый старт

Краткая инструкция по запуску проекта виртуальной примерки мебели.

## 📋 Предварительные требования

1. **Python 3.9+** установлен
2. **API ключи** получены:
   - OpenAI API key: https://platform.openai.com/api-keys
   - Replicate API token: https://replicate.com/account/api-tokens

## ⚡ Запуск за 5 минут

### 1. Клонируйте/откройте проект

```bash
cd "/Users/ilyshalit/Desktop/Примерка мебели"
```

### 2. Настройте переменные окружения

```bash
# Скопируйте пример
cp .env.example .env

# Откройте .env и вставьте ваши API ключи
nano .env  # или любой другой редактор
```

**Обязательно заполните:**
```env
OPENAI_API_KEY=sk-ваш-ключ-openai
REPLICATE_API_TOKEN=ваш-токен-replicate
```

### 3. Запустите приложение

```bash
# Используйте готовый скрипт (macOS/Linux)
./start.sh
```

**Или вручную:**

```bash
# Terminal 1: Backend
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app:app --reload

# Terminal 2: Frontend
cd frontend
python3 -m http.server 8080
```

### 4. Откройте в браузере

```
http://localhost:8080
```

## 🎯 Первое использование

1. **Загрузите фото комнаты** - любое фото интерьера
2. **Загрузите фото мебели** - диван, кресло, стол и т.д.
3. **Выберите режим:**
   - **Автоматически** - AI сам выберет место
   - **Вручную** - кликните где хотите разместить
4. **Нажмите "Создать визуализацию"**
5. **Ждите 8-15 секунд** ⏳
6. **Наслаждайтесь результатом!** 🎉

## 💰 Стоимость тестирования

- Первая генерация: ~$0.02
- 10 тестовых генераций: ~$0.20
- Для полноценной демонстрации заказчику: $5-10 достаточно

## ❓ Частые проблемы

### Ошибка: "OPENAI_API_KEY не установлен"

**Решение:** Проверьте что файл `.env` существует и содержит ключи.

### Ошибка: "Module not found"

**Решение:** 
```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

### Backend не запускается

**Решение:**
```bash
# Проверьте порт
lsof -i :8000

# Если занят, убейте процесс или измените порт
uvicorn app:app --reload --port 8001
```

### Долгая генерация (>30 сек)

**Возможные причины:**
- Медленный интернет
- API Replicate перегружен
- Изображения слишком большие (>2MB)

**Решение:** Уменьшите размер изображений до 1024x1024px

## 🔧 Дополнительные команды

```bash
# Проверка здоровья API
curl http://localhost:8000/api/health

# Просмотр API документации
open http://localhost:8000/docs

# Остановка всех процессов
pkill -f uvicorn
pkill -f "python.*http.server"
```

## 📚 Дальнейшие шаги

- 📖 Прочитайте полный [README.md](README.md)
- 🔄 Узнайте про [апгрейд до Варианта B](UPGRADE_TO_B.md)
- 🎨 Настройте интерфейс под свой бренд в `frontend/css/style.css`
- 📦 Добавьте товары в каталог через API

## 🆘 Нужна помощь?

- Проверьте логи в терминале где запущен backend
- Откройте DevTools в браузере (F12) → Console
- Проверьте `.env` файл

---

**Готово!** Теперь у вас работает система виртуальной примерки мебели! 🎉
